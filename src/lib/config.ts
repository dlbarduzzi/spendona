import { env } from "./env"

export const siteConfig = {
  url: env.APP_URL,
  name: "spendona",
  description: "Track subscriptions, monitor spending, and never miss a charge again. All in one place.",
}
